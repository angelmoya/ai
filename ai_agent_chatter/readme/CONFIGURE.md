1. Create an `ai.agent` record with a goal, soul, and connection.
2. Create a `res.users` and set the **AI Agent** field to your agent.
3. Start a direct chat with that user — the agent will auto-respond.
4. In group channels, @mention the AI user to trigger the agent.
5. The agent will see the document context and conversation history automatically.
