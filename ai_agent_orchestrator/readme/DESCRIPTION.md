# AI Agent Orchestrator

This module adds orchestrator agents that can delegate tasks to member agents.

An orchestrator agent breaks a user goal into steps, assigns each step to a
member agent by role, executes the member agents, and aggregates their results
into a final response.
