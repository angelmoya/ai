# AI Agent Multi-User

This module adds multi-user access control to the AI Agent module.

Administrators can define access rules that grant users or groups permission to
use specific agents. Agents without access rules remain visible to everyone;
once a rule exists, only matching users can see and use the agent.
